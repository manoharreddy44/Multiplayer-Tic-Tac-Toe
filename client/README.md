# Client (React + Vite)

This folder is reserved for the React frontend.

Planned responsibilities:
- Authenticate players with Nakama.
- Submit `MOVE` op-code messages only.
- Render state snapshots broadcast by the authoritative Nakama match.
